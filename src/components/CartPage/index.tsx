import Header from 'components/RestaurantPage/Header';

// Корзину переделать на модалку
const CartPage = () => {
  return (
    <div className="min-h-screen">
      <Header />
      <p className="text-center py-10 text-xl font-bold">Здесь будет корзина</p>
    </div>
  );
};

export default CartPage;
